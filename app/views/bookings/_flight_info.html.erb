for Flight number
<%= flight.id %>
which departs from
<%= flight.departure_airport.code %>
at
<%= flight.start.strftime("%H:%M")%>
on
<%= flight.flight_date %>
, lasting
<%= flight.duration_hours %>
<% if flight.duration_hours == 1 %>
  hour
<% else %>
  hours
<% end %>
and
<%= flight.duration_minutes %>
<% if flight.duration_minutes == 1 %>
  minute
<% else %>
  minutes
<% end %>
before arriving at
<%= flight.arrival_airport.code.concat('.') %>
<br>